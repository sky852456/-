# 旅行规划 · PWA 部署说明

## 这是什么

原来那个单文件 HTML 应用，现在加上了 PWA 能力——**可以添加到手机主屏幕，点开就是全屏应用，断网也能用**。

## 文件清单

```
部署包/
├── travel-planner.html          ← 主应用（已注入 PWA 支持）
├── manifest.webmanifest         ← 应用清单（名称/图标/全屏模式）
├── sw.js                        ← Service Worker（离线缓存）
├── icons/                       ← 图标（4 个尺寸）
│   ├── icon-192.png
│   ├── icon-512.png
│   ├── icon-maskable-512.png
│   └── apple-touch-icon.png
└── 示例数据-青甘大环线.json      ← 示例行程（可选，用于导入试用）
```

**这 6 个文件必须放在同一个目录里**，不要拆开，否则图标和离线功能会失效。

## 怎么用

### 方式一：本地直接用（最简单，但没有离线能力）

双击 `travel-planner.html` 就能用，功能和以前完全一样。

> ⚠️ 但 `file://` 协议下 Service Worker 无法注册，所以**没有离线缓存**。适合临时看看。

### 方式二：部署到网上（推荐，完整能力）

PWA 需要 **https** 才能启用离线能力。任选一个免费托管：

#### GitHub Pages（推荐，免费且稳定）

1. 注册/登录 [github.com](https://github.com)
2. 新建一个仓库，名字随意（如 `my-travel`），**选 Public**
3. 把 `部署包` 里的 **6 个文件**上传到仓库根目录
4. 进入仓库 → Settings → Pages → Source 选 `main` 分支 → 保存
5. 等 1-2 分钟，访问 `https://你的用户名.github.io/my-travel/travel-planner.html`

#### Vercel（更简单，拖拽即可）

1. 访问 [vercel.com](https://vercel.com) 用邮箱注册
2. 点 Add New → Project → 直接把 `部署包` 文件夹拖进去
3. 几十秒后拿到一个 `https://xxx.vercel.app` 链接

#### Netlify / Cloudflare Pages

同理，都是拖拽文件夹即可，都有免费额度。

### 方式三：添加到手机主屏幕

拿到 https 链接后：

**安卓（Chrome）**
1. 用 Chrome 打开链接
2. 底部会自动弹出「添加到主屏幕」提示条，点「添加」
3. 或者点右上角 ⋮ → 「添加到主屏幕」

**iPhone（Safari）**
1. **必须用 Safari**（Chrome 不行）
2. 打开链接后，点底部的**分享按钮**（方框带向上箭头）
3. 在菜单里向下滑，选**「添加到主屏幕」**
4. 右上角点「添加」

添加后，桌面会出现一个薄荷青的地图标记图标。点它打开——**没有地址栏，和原生 App 一样**。

## 离线能力说明

首次联网打开后，应用会自动把以下内容缓存到本地：

- 应用本体（HTML / 图标 / manifest）
- Leaflet 地图库
- 二维码、扫码库

之后**断网也能正常打开和编辑**，改动会存在本地，等联网后地图底图恢复正常。

**不会缓存**：地图瓦片、地理编码接口（这些需要实时联网）。

## 数据存储现状

现在数据存在**浏览器本地**（localStorage），换手机或清缓存会丢。

**建议做法**：
- 定期用应用内「⬇️ 导出备份」生成 JSON 文件
- 换设备时用「⬆️ 导入备份」恢复

> 云同步（登录账号、多设备自动同步）已经预留好接口，等接入后这部分就不用手动导了。

## 常见问题

**Q：为什么手机上点开还是浏览器界面，有地址栏？**
A：说明没有"添加到主屏幕"。必须通过上述方式添加后，从桌面图标打开才是全屏。

**Q：图标没显示/是默认图标？**
A：检查 `icons/` 文件夹是否和 html 在同一目录，且 4 个 png 都在。

**Q：改了文件但页面没更新？**
A：Service Worker 缓存导致。刷新时按住 Shift（桌面），或在手机浏览器设置里清除该站点数据。应用检测到新版本时会自动弹「✨ 有新版本可用」提示。

**Q：iPhone 上没看到安装提示？**
A：iOS 不支持自动安装提示。打开应用约 6 秒后会弹出图文教程，按提示操作即可。

---

## 验证记录

沙箱内用 Chromium 真实运行验证，结果：

| 检查项 | 结果 |
|---|---|
| manifest 解析（standalone / 中文名 / 4 图标） | ✅ |
| Service Worker 注册并 activated | ✅ |
| **断网后仍能打开应用** | ✅ body 29.9 万字符，知识库 25 条 |
| 图标 4 个全部可加载 | ✅ |
| 行程阶段推断（D1出发/D2中转/D10返程） | ✅ 10 天全部正确 |
| 知识库命中（青海湖/茶卡/莫高窟等） | ✅ 8/9（机场不属景点，未命中是正确设计） |
| 中转日自动推荐吃玩 | ✅ 兰州：吃5项 玩4项 |
| 采纳门票/时长写入 | ✅ `{"t":"","d":""}` → `{"t":"¥70","d":"2 小时"}` |
| 手机端不横向溢出 | ✅ 375/375 |
| JS 运行时错误 | ✅ 无 |
