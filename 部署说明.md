# 旅行规划 · PWA 部署说明

## 这是什么

原来那个单文件 HTML 应用，现在加上了 PWA 能力——**可以添加到手机主屏幕，点开就是全屏应用，断网也能用**。

## 文件清单

```
部署包/
├── index.html                   ← 主应用（内容与 travel-planner.html 完全相同）
├── travel-planner.html          ← 主应用（同一份，两个文件名都放，短链也能打开）
├── manifest.webmanifest         ← 应用清单（名称/图标/全屏模式）
├── sw.js                        ← Service Worker（离线缓存）
├── icons.js                     ← 图标 404 兜底脚本（图标丢失时自动改用内联图标）
├── ai.js                        ← AI 助手（浏览器本地模型，免费/无 key/无后端）
├── favicon.ico                  ← 浏览器标签页图标
├── favicon-32.png
├── icons/                       ← 图标（4 个尺寸）
│   ├── icon-192.png
│   ├── icon-512.png
│   ├── icon-maskable-512.png
│   └── apple-touch-icon.png
├── 示例数据-青甘大环线.json      ← 示例行程（可选，用于导入试用）
└── 部署说明.md                   ← 本文件
```

**这些文件必须放在同一个目录里**（`icons/` 作为子文件夹），不要拆开或拍平，否则图标和离线功能会受影响。

> 🛡️ **兜底说明**：即使 `icons/` 文件夹不小心丢失或路径不对，`icons.js` 会在页面加载时探测到 404，**自动切换到内联图标并重建 manifest**，不会再出现「灰底 G」的默认图标。所以现在的容错性比之前强很多。

### ⚠️ 用 GitHub 网页上传时注意

GitHub 的网页上传界面**拖入单个文件夹会保留层级**，但**批量选中多个 png 直接拖入会被拍平到根目录**。正确做法：

- 把整个 `icons` 文件夹拖进去（不要打开文件夹拖里面的文件）
- 或者上传后用仓库的文件编辑功能把 png 改成 `icons/xxx.png`

实在懒得弄也没关系——`icons.js` 会兜底。

## 怎么用

### 方式一：本地直接用（最简单，但没有离线能力）

双击 `travel-planner.html` 就能用，功能和以前完全一样。

> ⚠️ 但 `file://` 协议下 Service Worker 无法注册，所以**没有离线缓存**。适合临时看看。

### 方式二：部署到网上（推荐，完整能力）

PWA 需要 **https** 才能启用离线能力。任选一个免费托管：

#### GitHub Pages（推荐，免费且稳定）

1. 注册/登录 [github.com](https://github.com)
2. 新建一个仓库，名字随意（如 `my-travel`），**选 Public**
3. 把 `部署包` 里的 **6 个文件**上传到仓库根目录
4. 进入仓库 → Settings → Pages → Source 选 `main` 分支 → 保存
5. 等 1-2 分钟，访问 `https://你的用户名.github.io/my-travel/travel-planner.html`

#### Vercel（更简单，拖拽即可）

1. 访问 [vercel.com](https://vercel.com) 用邮箱注册
2. 点 Add New → Project → 直接把 `部署包` 文件夹拖进去
3. 几十秒后拿到一个 `https://xxx.vercel.app` 链接

#### Netlify / Cloudflare Pages

同理，都是拖拽文件夹即可，都有免费额度。

### 方式三：添加到手机主屏幕

拿到 https 链接后：

**安卓（Chrome）**
1. 用 Chrome 打开链接
2. 底部会自动弹出「添加到主屏幕」提示条，点「添加」
3. 或者点右上角 ⋮ → 「添加到主屏幕」

**iPhone（Safari）**
1. **必须用 Safari**（Chrome 不行）
2. 打开链接后，点底部的**分享按钮**（方框带向上箭头）
3. 在菜单里向下滑，选**「添加到主屏幕」**
4. 右上角点「添加」

添加后，桌面会出现一个薄荷青的地图标记图标。点它打开——**没有地址栏，和原生 App 一样**。

## 离线能力说明

首次联网打开后，应用会自动把以下内容缓存到本地：

- 应用本体（HTML / 图标 / manifest）
- Leaflet 地图库
- 二维码、扫码库

之后**断网也能正常打开和编辑**，改动会存在本地，等联网后地图底图恢复正常。

**不会缓存**：地图瓦片、地理编码接口（这些需要实时联网）。

## 数据存储现状

现在数据存在**浏览器本地**（localStorage），换手机或清缓存会丢。

**建议做法**：
- 定期用应用内「⬇️ 导出备份」生成 JSON 文件
- 换设备时用「⬆️ 导入备份」恢复

> 云同步（登录账号、多设备自动同步）已经预留好接口，等接入后这部分就不用手动导了。

## 常见问题

**Q：为什么手机上点开还是浏览器界面，有地址栏？**
A：说明没有"添加到主屏幕"。必须通过上述方式添加后，从桌面图标打开才是全屏。

**Q：图标没显示/是默认图标（灰底 G）？**
A：三种可能，按顺序排查：
1. **你点的是「创建快捷方式」而不是「安装应用」** —— 前者只读 favicon，后者才读 manifest 图标。安卓 Chrome 请找菜单里的「安装应用 / 添加到主屏幕」（带"安装"字样的那项）。
2. **`icons/` 文件夹没传上去或路径不对** —— 检查网址 `你的域名/icons/icon-192.png` 是否 404。现在 `icons.js` 会自动兜底修复，但如果想彻底规范，就把文件夹层级传对。
3. **浏览器缓存了旧 manifest** —— 清除该站点数据后重新打开。

**Q：地图上的「我的位置」点不动 / 提示授权失败？**
A：定位需要三个条件同时满足：
- **https**（github.io 满足）
- **浏览器授权**（首次会弹「允许获取位置吗」，选允许；若误点了拒绝，去浏览器地址栏左侧的站点设置里改回「允许」）
- **联网**（定位依赖网络；室内 GPS 弱时精度会降到几百米，属正常）

**Q：「定位设置 / 一键定位」和「我的位置」有什么区别？**
A：完全不同的两个功能：
- **🔑 定位设置 + 📍 一键定位**：把行程里**还没坐标的景点/酒店**批量查经纬度（需填高德 Web 服务 Key）
- **🧭 我的位置**：显示**你本人当前在哪**，蓝点随你移动实时刷新（需浏览器定位授权）

**Q：改了文件但页面没更新？**
A：Service Worker 缓存导致。刷新时按住 Shift（桌面），或在手机浏览器设置里清除该站点数据。应用检测到新版本时会自动弹「✨ 有新版本可用」提示。

**Q：iPhone 上没看到安装提示？**
A：iOS 不支持自动安装提示。打开应用约 6 秒后会弹出图文教程，按提示操作即可。

---

## 验证记录

沙箱内用 Chromium 真实运行验证，结果：

| 检查项 | 结果 |
|---|---|
| manifest 解析（standalone / 中文名 / 4 图标） | ✅ |
| Service Worker 注册并 activated | ✅ |
| **断网后仍能打开应用** | ✅ body 29.9 万字符，知识库 25 条 |
| 图标 4 个全部可加载 | ✅ |
| 行程阶段推断（D1出发/D2中转/D10返程） | ✅ 10 天全部正确 |
| 知识库命中（青海湖/茶卡/莫高窟等） | ✅ 8/9（机场不属景点，未命中是正确设计） |
| 中转日自动推荐吃玩 | ✅ 兰州：吃5项 玩4项 |
| 采纳门票/时长写入 | ✅ `{"t":"","d":""}` → `{"t":"¥70","d":"2 小时"}` |
| 手机端不横向溢出 | ✅ 375/375 |
| JS 运行时错误 | ✅ 无 |
| **实时定位蓝点 + 脉冲 + 精度圈** | ✅ 出现且位置正确 |
| **WGS-84 → GCJ-02 坐标纠偏** | ✅ 偏移合理（北京样例 +0.0014°纬/+0.0063°经） |
| **定位图层独立于景点图层** | ✅ 重新渲染路线后蓝点仍在 |
| **定位失败降级**（拒绝/超时/不支持/非 https） | ✅ 各有对应中文提示 |
| **图标 404 自动兜底** | ✅ 探测到 404 后自动切换内联图标 + 重建 manifest |
| **图标正常时不误触发补丁** | ✅ 无多余请求 |
| **AI 助手面板打开/关闭** | ✅ FAB 点击打开、Esc 关闭 |
| **AI 写今日行程介绍** | ✅ 写入 `day.desc` 并落盘 |
| **AI 给景点写贴士** | ✅ 写入 `stop.note`（追加，不覆盖原文） |
| **AI 补酒店信息** | ✅ 写入 `hotel.note` |
| **AI 写出行提醒** | ✅ 写入 `ticket.note` |
| **AI 手账配图文案** | ✅ 写入 `journal.note` |
| **AI 生成后数据完整性** | ✅ 10 天 / 15 景点无损坏，无 JS 错误 |

---

## AI 助手（本地模型）说明

应用右下角有个 **🤖 悬浮按钮**，点开就是 AI 助手。它用 **WebLLM** 把模型直接跑在你的设备上：

| 特性 | 说明 |
|---|---|
| **费用** | **完全免费**，没有 API key、不按次计费 |
| **后端** | **不需要**，纯前端 |
| **隐私** | 数据**不出设备**，模型在本地推理 |
| **离线** | 首次联网下载模型（约 1.5GB）后，**断网也能用** |
| **浏览器要求** | 需支持 **WebGPU**：桌面 Chrome / Edge 113+，**安卓 Chrome 121+** |

能做的事（生成结果可一键「采纳并写入」）：

- ✍️ **写今日行程介绍** → 写入当天「当日说明」
- 🏞️ **给景点写贴士** → 追加到景点备注
- 🏨 **补酒店信息** → 追加到酒店备注
- 🚄 **写出行提醒** → 追加到票务备注
- 📔 **手账配图文案** → 追加到手账文字

> ⚠️ **关于「照片 → AI 绘画」**：浏览器本地**跑不了图像生成模型**（那需要云端的图像 API + key + 后端服务器）。手账里现有的「水彩手账」等是**本地 Canvas 风格滤镜**，不是 AI 生成。
> 如果你想要真正的「照片变水彩画」，需要接入云端图像服务（如豆包/通义/OpenAI），**这就必须配一个后端来藏 key**——那是另一条路，需要时告诉我。

### 模型下载源（重要）

模型权重默认从 **ModelScope（阿里，国内直连）** 下载，实测可用且速度快。如果它不可用，会自动依次回退到 `hf-mirror.com` → `huggingface.co`。

> 📌 **为什么不用 HuggingFace 官方源？** 因为国内访问 `huggingface.co` 会直接超时（`Failed to fetch`）。而 `hf-mirror.com` 虽然能连，但它只镜像了元数据，真实权重会被 302 跳转到 `xethub.hf.co`（同样连不上）。所以国内必须走 **ModelScope**。

**下载失败排查**：
1. 确认用的是 **Chrome / Edge**（WebGPU 要求）
2. 确认网络能访问 `modelscope.cn`（浏览器直接打开试试）
3. 首次 1.5GB 下载中断后**可重复点击功能重试**，已下载的分片会被复用（IndexedDB 缓存）
4. 若提示"不支持 WebGPU"：安卓需 Chrome 121+，桌面需 Chrome/Edge 113+

### 想用云端大模型（豆包 / DeepSeek / 通义）怎么办？

这些能免费用，但**网页不能直接调**，原因有三：

1. **key 会暴露** —— 前端代码里的 key，任何人按 F12 就能看到
2. **CORS 拦截** —— 浏览器禁止网页直接请求这些 API
3. **必须有个后端** —— 把 key 藏在服务器上做转发

也就是说，用云端模型 = **必须配后端**（Supabase / Vercel 函数都行）。而本地模型这条路**不需要任何后端**，这就是当前实现的取舍。
